#include <complex>
#include <vector>

#include "clone_entity.hpp"
#include "gtest/gtest.h"
#include "config.h"

TEST(Entity, Test1) {
    const char *filepath = getRootPath();
    std::cout << "File Path :- " << filepath << std::endl;
}

TEST(Entity, Test2) {
    const char *filepath = getRootPath();
    std::cout << "File Path :- " << filepath << std::endl;
}

TEST(Entity, Test3) {
    const char *filepath = getRootPath();
    std::cout << "File Path :- " << filepath << std::endl;
}
